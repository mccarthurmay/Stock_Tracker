# Research module deps (separate from the legacy backend, which pins
# alpaca-trade-api). M2 will add py_vollib / QuantLib / scipy for Greeks/IV.
alpaca-py>=0.30.0
duckdb>=1.0.0
pandas>=2.0.0
python-dotenv>=1.0.0
